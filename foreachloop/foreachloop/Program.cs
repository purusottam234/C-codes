using System;

namespace foreachloop
{
    class Program
    {
        static void Main(string[] args)
        {/*
            string[] Country = { "Nepal", "China", "India", "USA" };
            foreach(String x in Country){
                Console.WriteLine(x);*/

            Int32[] Just = { 1, 2, 3, 4, 5 };
            foreach (Int32 a in Just)
            {
                Console.WriteLine(a);
            }
        }
    
    }
}
